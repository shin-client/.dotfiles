return {
  "norcalli/nvim-colorizer.lua",
  opts = {
    user_default_options = {
      RGB = true, -- Hiển thị màu RGB.
      RRGGBB = true, -- Hiển thị màu Hex (RRGGBB).
      names = true, -- Hiển thị các tên màu (ví dụ: `red`, `blue`,...).
      RRGGBBAA = true, -- Hiển thị mã Hex có độ trong suốt (RGBA).
      hsl = true, -- Hiển thị màu HSL.
      css = true, -- Hiển thị màu CSS.
      css_fn = true, -- Hiển thị màu trong các hàm CSS (ví dụ: `rgb()`, `hsl()`).
    },
  },
}
